const rss = require('rss-parser')

function FeedParser() {
    if (!new.target) {
        return new FeedParser()
    }

    this.parse = (urls) => {
        'http://rss1'
            ('http://rss1', 'http://rss2', 'http://rss3')
        return [{}]
    }
}

module.exports = FeedParser 